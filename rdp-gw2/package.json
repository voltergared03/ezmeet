{
  "name": "garely-guac-tunnel",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "description": "Garely RDP v2 tunnel — guacamole-lite WebSocket <-> guacd bridge. The browser connects with a Garely-minted AES-256-CBC token that carries the target host + injected vault credentials; the tunnel decrypts it server-side and hands it to guacd, so the RDP password never reaches the browser.",
  "dependencies": {
    "guacamole-lite": "^1.2.0"
  }
}
